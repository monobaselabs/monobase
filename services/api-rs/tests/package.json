{
  "name": "@monobase/api-rs-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "E2E tests for the Monobase Rust API service",
  "scripts": {
    "test": "bun test",
    "deps:up": "docker compose up -d --wait",
    "deps:down": "docker compose down -v"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.0.0",
    "@types/bun": "latest",
    "@types/pg": "^8.15.5",
    "pg": "^8.16.3"
  }
}
